// src/app/ui/ds/tint.ts
// Pre-formatted with underscores instead of spaces so these can be dropped
// directly into a Tailwind arbitrary-value class, e.g. `bg-[${HOVER_TINT}]`.
export const HOVER_TINT = "color-mix(in_srgb,var(--color-text)_7%,transparent)";
export const ACTIVE_TINT = "color-mix(in_srgb,var(--color-text)_14%,transparent)";
